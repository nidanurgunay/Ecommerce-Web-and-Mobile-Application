import Encoding from './Encoding';
export { Encoding };
export interface CustomSettings {
    lowerCaseLetters: boolean;
    upperCaseLetters: boolean;
    numbers: boolean;
    symbols: boolean;
}
export default function generatePassword(length: number, encoding?: Encoding | CustomSettings): Promise<string>;
